PLayers
<% @users.each do |user| %>
  <div>
    <%= user.email %>
    <% #if user_signed_in? and user.email == current_user.email %>
      <%= link_to "Request a game", edit_game_path(12), :remote => true  %> 
    <% #end %>
  <div>
<% end %>